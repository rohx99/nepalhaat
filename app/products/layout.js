export const metadata = {
  title: "Products | NepalHaat",
  description: "This page provide a list of all products",
};

export default function RootSignUp({ children }) {
  return <>{children}</>;
}
