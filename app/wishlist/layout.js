export const metadata = {
  title: "Wishlist | NepalHaat",
  description:
    "This page provides details of all products wishlisted by the customer.",
};

export default function RootSignUp({ children }) {
  return <>{children}</>;
}
