export const metadata = {
  title: "Orders | NepalHaat",
  description:
    "This page provides customer the ability to manage their orders.",
};

export default function RootProfile({ children }) {
  return <>{children}</>;
}
