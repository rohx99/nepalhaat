export const metadata = {
  title: "Profile | NepalHaat",
  description:
    "This page provides the ability to customer to update their personal information",
};

export default function RootProfile({ children }) {
  return <>{children}</>;
}
